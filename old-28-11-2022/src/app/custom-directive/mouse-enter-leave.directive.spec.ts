import { MouseEnterLeaveDirective } from './mouse-enter-leave.directive';

describe('MouseEnterLeaveDirective', () => {
  it('should create an instance', () => {
    const directive = new MouseEnterLeaveDirective();
    expect(directive).toBeTruthy();
  });
});
